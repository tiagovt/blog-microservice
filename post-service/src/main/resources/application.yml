server:
  port: ${PORT:8080}
  #contextPath: /configclient

logging:
  levels:
    org.springframework.boot.env.PropertySourcesLoader: TRACE
    org.springframework.web: DEBUG
    
endpoints:
  restart:
    enabled: true
    
spring:
  oauth2:
    resource:
      userInfoUri: http://localhost:9999/uaa/user    

#spring:
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
